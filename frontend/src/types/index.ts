/**
 * Barrel export for all shared type definitions.
 *
 * @example
 * ```ts
 * import { PolicyTemplate, TemplateField } from '../types';
 * ```
 */
export type {
  PolicyTemplate,
  TemplateField,
  TemplateFieldType,
} from './PolicyTemplate';
