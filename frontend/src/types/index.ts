/**
 * Barrel export for all shared type definitions.
 *
 * @example
 * ```ts
 * import { FieldTemplate, TemplateField } from '../types';
 * ```
 */
export type {
  FieldTemplate,
  TemplateField,
  TemplateFieldType,
} from './FieldTemplate';
