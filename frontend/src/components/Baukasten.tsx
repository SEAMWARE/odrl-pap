/**
 * Backward-compatible re-export of PolicyBuilder.
 *
 * The "Baukasten" name is deprecated in favor of "PolicyBuilder".
 * This module exists so that existing imports continue to work
 * during the transition.
 *
 * @deprecated Use `PolicyBuilder` from `./PolicyBuilder` instead.
 */
export { default } from './PolicyBuilder';
